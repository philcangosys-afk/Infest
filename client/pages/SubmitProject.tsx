import SubmitProjectStep1 from "./SubmitProjectStep1";

// This wrapper page can later be split into routes for each step
// For now, defaulting to Step 1
export default function SubmitProject() {
  return <SubmitProjectStep1 />;
}
